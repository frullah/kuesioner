<%= bootstrap_form_with(model: mata_kuliah, local: true, class: "mb-4") do |form| %>

  <%= form.text_field :nama %>

  <%= form.telephone_field :sks %>

  <%= form.telephone_field :semester %>

  <div class="actions">
    <%= form.primary %>
  </div>
<% end %>